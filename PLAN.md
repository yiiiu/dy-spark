**视频队列转发模式**

**总结**
- 新增“视频模式”：开启后，定时任务使用队列中的当前视频转发给续火花好友。
- 视频发送成功后不再发送文字；原生卡片失败时自动降级为视频链接文本。
- 视频队列支持手动粘贴链接，也支持从最近点赞的 20 条视频中勾选加入。
- 每个批次所有好友发送同一条视频；下一批次自动使用队列下一条。

**实现调整**
- `core/config.py`
  - 增加视频模式开关。
  - 增加有序视频队列，保存 URL、标题、来源和加入时间。
  - 对 URL 做格式校验、标准化和去重。
- `core/runtime.py`
  - 保存当前队列游标。
  - 保存“视频 + 好友”的发送历史。
  - 保存失败好友的视频重试信息。
- `core/automation.py`
  - 新增读取最近点赞视频逻辑，最多返回 20 条。
  - 新增原生视频分享卡片发送流程。
  - 卡片操作失败时，自动调用现有聊天输入框发送视频链接。
  - 每批次固定使用同一条视频；已成功发送过的“视频 + 好友”组合跳过。
- `app.py`
  - 增加点赞列表读取接口。
  - 增加视频队列查询、添加、删除、排序接口。
  - 扩展运行接口，支持文字模式、视频模式和干跑测试。
  - 保留现有文字续火花接口和行为不变。
- `static/index.html`
  - 增加“视频转发”页签。
  - 支持每行一个 URL 批量添加。
  - 支持读取最近点赞、勾选视频并加入队列。
  - 显示队列顺序、当前视频、下一条视频、发送历史和失败状态。
  - 提供“立即发送当前视频”和干跑测试按钮。
  - 增加视频模式开关。

**批次规则**
- 定时任务开启视频模式后：
  1. 取队列当前视频。
  2. 向本批次好友发送同一视频。
  3. 成功记录好友发送历史。
  4. 卡片失败则发送链接文本并标记降级。
  5. 初始批次结束后队列游标前进。
  6. 失败好友继续使用现有补发机制，并携带原视频 ID，避免错误发送下一条视频。
- 视频队列为空或视频发送失败且链接降级也失败时，本批次回退到原有文字消息。

**测试与验收**
- 配置保存、URL 标准化、重复 URL 去重。
- 最近点赞读取和最多 20 条限制。
- 队列游标按批次推进。
- 同一视频发送给多个好友时只跳过已成功的好友。
- 原生卡片失败后正确降级为链接文本。
- 失败好友重试时仍使用原视频。
- 视频模式关闭时，现有文字续火花流程保持不变。
- API 和 Playwright 流程使用 mock 测试，不依赖真实页面即可验证核心逻辑。

**默认假设**
- 手动立即发送会消耗当前队列视频并推进队列。
- 点赞视频只在用户点击读取并勾选后加入队列，不自动追加。
- 队列中的链接以抖音视频 URL 为主，标题和封面仅作为界面展示信息。
